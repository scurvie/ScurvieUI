project('ScurvieUI', 'c', 'cpp',
  default_options : [ 'cpp_std=c++20']
)
version = '0.0.1'
soversion = '0'
pkg = import('pkgconfig')
incdir = include_directories('Libraries')

subdir('Libraries')
subdir('src')
