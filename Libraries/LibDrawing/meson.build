LibDrawing = library('LibDrawing',
  'Color.cpp',
  version : version,
  soversion : soversion,
  include_directories : incdir,
  install : true )
install_headers(
  'Color.h',
  subdir : 'LibDrawing' )
pkg.generate(LibDrawing)
